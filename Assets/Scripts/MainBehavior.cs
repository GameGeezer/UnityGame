using UnityEngine;
using System;

public class MainBehavior : MonoBehaviour {

	// Use this for initialization
	void Start () {
       // KDTree<int> tree = new KDTree<int>();
        //tree.Insert(2, new Vector3(1, 1, 1));
        //tree.Insert(2, new Vector3(0, 0, 0));

       // tree.Refresh();

        //KDTreeEntry<int> n = tree.NearestNeighbor(new Vector3 (1, 0, 1));
    }
	
	// Update is called once per frame
	void Update () {

    }
}
